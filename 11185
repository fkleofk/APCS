#include<iostream>
#include<math.h>
using namespace std;
int f(int n){
	if(n<3){
		return n;
	}
	else{
		int tmp = n%3;
		return (f(n/3)*10 + tmp);
	}
}
#ifdef local
	

int main() {
	long long int n ;
	while(cin>>n) {
		if (n<0)
			break;
		else
			cout << f(n) << endl ;
			}
	return 0;
}
